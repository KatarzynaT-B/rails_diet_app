<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, 'Tytuł' %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :author, 'Autor' %><br>
    <%= f.text_field :author %>
  </div>
  <div class="field">
    <%= f.label :source, 'Źródło' %><br>
    <%= f.text_field :source %>
  </div>
  <div class="field">
    <%= f.label :content, 'Treść' %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Zapisz artykuł" %>
  </div>
<% end %>
